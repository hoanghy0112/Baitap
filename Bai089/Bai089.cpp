#include <iostream>
#include <cmath>

using namespace std;

int main()
{
	int n;
	cin >> n;
	float s = 0;
	int t = 1;
	int m = 0;
	for (int i = 1; i <= n; i += 1)
	{
		t = t * (-x);
		m = m + i;
		s = s + float(t) / m;
	}

	cout << s;

	return 0;
}